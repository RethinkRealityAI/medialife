import { createFileRoute } from "@tanstack/react-router";

// Placeholder: replaced by the Analytics page.
export const Route = createFileRoute("/admin/analytics")({
  component: () => <div className="p-6 text-sm text-muted-foreground">Analytics: coming soon.</div>,
});
