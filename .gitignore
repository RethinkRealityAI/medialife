# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Netlify
.netlify/

# Local-only harness for eyeballing product GLBs and decal placement. It is
# served from public/ so it shares the creators importmap, but it is scratch
# tooling and must never ship.
public/_glbtest/

# Raw multi-MB generated GLBs. scripts/optimize-models.mjs turns these into the
# web-ready models under public/creators/assets/models/, which are committed.
assets-src/
